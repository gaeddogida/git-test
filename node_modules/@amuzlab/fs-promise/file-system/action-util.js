'use strict'

Object.defineProperties(
    exports,
    {
        callListener: {
            enumerable: true,
            value: (listeners, eventName, ...args) => {
                if (listeners && listeners[eventName]) {
                    listeners[eventName](...args)
                }
            }
        }
    })
